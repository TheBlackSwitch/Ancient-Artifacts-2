

> graves[v] = graves vanilla tweaks
> graves[l] = lullaby's graves

# Graves compat methods:

1. Overrride the loot table:

    ** PROS **
    - Easy
    - Minimal effort
    - Very stable

    ** CONS **
    - Requires the user to change the load order so AA loads after graves[V]
    - Not compatable with anything that edits the player loot table (like player drops heads)